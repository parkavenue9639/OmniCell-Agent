请审阅附带的分析图像。

用户目标：
{user_messages_text}

当前步骤：
{plan}

只依据图像可见内容评估图型匹配、渲染完整性和可读性，不推断图外的统计或生物学结论。返回以下 JSON，不附加 Markdown：
{{
    "status": "success" 或 "error",
    "feedback": "success 时简述已核对的可见要素；error 时说明具体可见缺陷与完成当前绘图步骤所需的最小修复。"
}}
